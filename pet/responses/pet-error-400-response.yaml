description: Bad Request
content:
  application/json:
    schema:
      type: object
      properties:
        errorCode:
          type: string
          example: PET_400
        message:
          type: string
          example: "Invalid request data."
        details:
          type: string
          example: "Missing required pet type."